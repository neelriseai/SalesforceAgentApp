# Complete manual regression suite

Generated by `npm run manual:build` from the versioned scenario specifications. Do not hand-edit this file. Every case is **NOT RUN** until executed and evidenced separately.

Read [test plan](test-plan.md) and [configuration guide](demo-rules-guide.md) first. The suite contains 42 cases and 232 numbered action/expected-result steps. `{run}` means a unique locally recorded suffix, for example `demo01`; never blindly reuse a consumed Lead or create name.

| ID | Objective | Steps |
|---|---|---:|
| [MM-01](#mm-01) | Account hierarchy and subsidiary reassignment | 5 |
| [MM-02](#mm-02) | Contact reassignment between customer accounts | 5 |
| [MM-03](#mm-03) | Lead qualification and conversion into a customer deal | 6 |
| [MM-04](#mm-04) | Opportunity pipeline progression without policy-input changes | 5 |
| [MM-05](#mm-05) | Buying-team contact roles and primary contact | 5 |
| [MM-06](#mm-06) | Customer support case triage and closure | 5 |
| [MM-07](#mm-07) | Case follow-up task and activity completion | 5 |
| [MM-08](#mm-08) | Campaign member response tracking | 5 |
| [MM-09](#mm-09) | User separation, assigned approval and rejection | 6 |
| [MM-10](#mm-10) | Customer 360 relationship reconciliation | 6 |
| [POLICY-01](#policy-01) | Non Strategic | 6 |
| [POLICY-02](#policy-02) | Exact Amount | 6 |
| [POLICY-03](#policy-03) | Exact 15 Percent | 6 |
| [POLICY-04](#policy-04) | Above 15 Percent | 6 |
| [POLICY-05](#policy-05) | Missing Approver | 6 |
| [POLICY-06](#policy-06) | Null Discount | 6 |
| [POLICY-07](#policy-07) | Null Amount | 6 |
| [RULE-01](#rule-01) | Account reparent blocked, then allowed by switch | 7 |
| [RULE-02](#rule-02) | Contact account change blocked and case links preserved | 6 |
| [RULE-03](#rule-03) | Native lead conversion qualification gate and rollback | 6 |
| [RULE-04](#rule-04) | Closed Won minimum: 99999.99 USD is blocked | 5 |
| [RULE-05](#rule-05) | Closed Won minimum: 100000 USD is allowed | 5 |
| [RULE-06](#rule-06) | Closed Won minimum: 100000.01 USD is allowed | 5 |
| [RULE-07](#rule-07) | Closed Won minimum: blank is blocked | 5 |
| [RULE-08](#rule-08) | Minimum changes immediately for the next save; switch-off restores permissive behavior | 7 |
| [RULE-09](#rule-09) | Primary contact gate at Opportunity save | 7 |
| [RULE-10](#rule-10) | Case resolution: blank and whitespace fail; text passes | 7 |
| [RULE-11](#rule-11) | Task completion requires a due date, not a future due date | 8 |
| [RULE-12](#rule-12) | Inactive campaign response blocked without sending messages | 8 |
| [RULE-13](#rule-13) | Two Opportunity guards compose without weakening either | 6 |
| [RULE-14](#rule-14) | Invalid enabled configuration fails closed | 6 |
| [RULE-15](#rule-15) | Lead required status is configurable | 5 |
| [RULE-16](#rule-16) | Renaming in the same save does not evade a reparent guard | 4 |
| [RULE-17](#rule-17) | Disabled rules preserve original workflows | 3 |
| [POLICY-08](#policy-08) | Discount configuration change from 15 to 20 with independent expected behavior | 6 |
| [POLICY-09](#policy-09) | Inactive strategic policy produces configuration error, not approval bypass | 3 |
| [APPROVAL-01](#approval-01) | Native approval approve with separate actors | 5 |
| [APPROVAL-02](#approval-02) | Native approval reject with separate actors | 5 |
| [APPROVAL-03](#approval-03) | Native approval recall with separate actors | 5 |
| [SECURITY-01](#security-01) | VP read-only inputs and no metadata-admin access | 4 |
| [SECURITY-02](#security-02) | Non-synthetic records remain outside demo guard scope | 4 |
| [RECOVERY-01](#recovery-01) | Rule restoration and dataset reconciliation | 5 |

<a id="mm-01"></a>

## MM-01 — Account hierarchy and subsidiary reassignment

Objective: Verify that changing a subsidiary's parent updates the account hierarchy without moving its contacts or deals.

Actor: Original demo administrator (business-operator stand-in)

Fixtures: `SYN-MM-A06 Customer`, `SYN-MM-A02 Group`, `SYN-MM-A01 Group`.

Preconditions: All eight Demo Business Rules disabled; strategic baseline-15 restored. A06 belongs to A02; A01 and A02 each have five seeded children. A06 has contacts C04–C06 and deals O03–O04.

| Step | Action | Expected result |
|---:|---|---|
| 1 | Open Accounts, choose All Accounts, search SYN-MM-A06 and open the Customer record. | Account Name is SYN-MM-A06 Customer; Parent Account is SYN-MM-A02 Group. |
| 2 | Use the account's View Account Hierarchy action (often in the action dropdown). | A06 is shown under A02. |
| 3 | Edit A06. Change only Parent Account to SYN-MM-A01 Group and save. | A06 saves under A01; no new Account is created. |
| 4 | Reopen the hierarchy and A06's Related tab. | A01 now has six seeded children and A02 four. A06 still has contacts C04–C06 and deals O03–O04. |
| 5 | Reload the record and inspect Parent Account again. | The changed parent persists; the account retains its original name and marker. |

Negative/safety check: Do not select a non-SYN account as parent. Moving a parent is not an instruction to move contact/account lookups.

Cleanup and repeatability: Restore A06 Parent Account to SYN-MM-A02 Group; confirm five seeded children under each group.

Capture: Before/after hierarchy and the unchanged contact/deal lists.

Manual result: NOT RUN. Copy this case ID into the run ledger; generated specifications never certify execution.

<a id="mm-02"></a>

## MM-02 — Contact reassignment between customer accounts

Objective: Verify contact-to-account reassignment and detect that an existing case's Account is not automatically rewritten.

Actor: Original demo administrator

Fixtures: `Synthetic SYN-MM-C04`, `SYN-MM-A06 Customer`, `SYN-MM-A07 Customer`, `SYN-MM-S04 Support`.

Preconditions: All eight Demo Business Rules disabled; strategic baseline-15 restored. C04 belongs to A06. S04 links C04 and A06. A06 and A07 each have three seeded contacts.

| Step | Action | Expected result |
|---:|---|---|
| 1 | Open Contacts, use an all-records list and search SYN-MM-C04. | The record is Synthetic SYN-MM-C04; Account is A06; the email ends in example.invalid. |
| 2 | Edit the Contact's Account Name to SYN-MM-A07 Customer; leave Name and Description unchanged; save. | C04 now links A07. |
| 3 | Open A06 and A07 > Related > Contacts. | A06 has two seeded contacts; A07 has four. C04 appears under A07. |
| 4 | Open SYN-MM-S04 Support and inspect Contact Name and Account Name. | Contact remains C04; Case Account remains A06. Native contact reassignment does not promise a case-account cascade. |
| 5 | Reload C04 and then restore its Account to A06. | C04 returns to A06 and both accounts again have three seeded contacts. |

Negative/safety check: If the business wants automatic case-account alignment, that is a new rule to design—not behavior to assume from this fixture.

Cleanup and repeatability: Restore C04 to A06. Do not modify S04's customer relationship during this test.

Capture: Contact and both account lists before/after; S04's unchanged relationships.

Manual result: NOT RUN. Copy this case ID into the run ledger; generated specifications never certify execution.

<a id="mm-03"></a>

## MM-03 — Lead qualification and conversion into a customer deal

Objective: Verify native lead conversion links an existing customer to a new contact and a new synthetic Opportunity.

Actor: Original demo administrator

Fixtures: `Synthetic SYN-MM-L30`, `SYN-MM-A24 Customer`.

Preconditions: All eight Demo Business Rules disabled; strategic baseline-15 restored. Run this test LAST. L30 is unconverted and is not one of the seeded campaign members. Conversion is a one-way business operation in the normal UI.

| Step | Action | Expected result |
|---:|---|---|
| 1 | Open Leads, choose All Open Leads or an equivalent all-records list, and find SYN-MM-L30. | Lead is Synthetic SYN-MM-L30; Company is SYN-MM-L30 Prospect; Status is Open - Not Contacted. |
| 2 | Edit Status to Working - Contacted and save. | The qualification state persists; the record is still a Lead. |
| 3 | Use Convert. Select existing Account SYN-MM-A24 Customer, choose to create a new Contact, and create a new Opportunity named SYN-MM-L30 Converted Deal. | The conversion form identifies only the synthetic customer and new synthetic records. Disable optional notification-email choices if shown. |
| 4 | Confirm Convert and open the resulting Account, Contact and Opportunity links. | Exactly one new Contact and one new Opportunity are linked to A24; no additional Account was requested. |
| 5 | Inspect the converted Lead or conversion confirmation and the new Opportunity. | Lead is converted. The Opportunity is named SYN-MM-L30 Converted Deal; its Account is A24. With no qualifying strategic inputs entered, policy status is Not Required. |
| 6 | Search the ordinary open-lead list for L30, then inspect A24's Related lists. | L30 no longer appears as an open Lead. A24 has the new Contact and Opportunity in addition to its original fixtures. |

Negative/safety check: Do not convert twice or reuse a similarly named real prospect. If duplicate detection appears, review the exact synthetic matches instead of bypassing it blindly.

Cleanup and repeatability: No automatic unconvert or deletion. Record the conversion as consumed; use unconverted L29, then L28, on later runs with corresponding unique Opportunity names. Verify reports Lead status drift intentionally.

Capture: Conversion confirmation plus the three linked records; capture only synthetic record details.

Manual result: NOT RUN. Copy this case ID into the run ledger; generated specifications never certify execution.

<a id="mm-04"></a>

## MM-04 — Opportunity pipeline progression without policy-input changes

Objective: Verify sales-stage changes persist while the strategic policy and evaluation count stay unchanged when policy inputs did not change.

Actor: Original demo administrator

Fixtures: `SYN-MM-O01 Deal`, `SYN-MM-A05 Customer`.

Preconditions: All eight Demo Business Rules disabled; strategic baseline-15 restored. O01: Prospecting, USD 101,000, 5% discount, strategic=false, policy Not Required, one initial evaluation.

| Step | Action | Expected result |
|---:|---|---|
| 1 | Open Opportunities in Strategic Deal Assurance; use All Opportunities and search SYN-MM-O01. | The workbench shows the expected input values and Not Required. |
| 2 | Record the existing evaluation-history count and newest timestamp. Change only Stage to Qualification; click Save and Evaluate. | Stage becomes Qualification; policy remains Not Required; no new policy evaluation is created. |
| 3 | Change only Stage to Closed Won and save. | Stage is Closed Won. Amount stays USD 101,000 and policy stays Not Required. |
| 4 | Reload the Opportunity and inspect its Account's related Opportunities. | The same deal appears as Closed Won under A05; no duplicate Opportunity was created. |
| 5 | Compare evaluation history with the first step. | The count and newest evaluation timestamp are unchanged. A stage-only change is not a strategic-input change. |

Negative/safety check: Do not change Amount, Discount, Strategic Deal or Regional VP Approver in this test: those are policy inputs and would create evaluation evidence.

Cleanup and repeatability: Restore Stage to Prospecting, leaving the other values unchanged.

Capture: Before/after stage, amount, policy status and evaluation history.

Manual result: NOT RUN. Copy this case ID into the run ledger; generated specifications never certify execution.

<a id="mm-05"></a>

## MM-05 — Buying-team contact roles and primary contact

Objective: Verify a deal can retain multiple buying-team contacts while changing its primary contact and role.

Actor: Original demo administrator

Fixtures: `SYN-MM-O03 Deal`, `Synthetic SYN-MM-C04`, `Synthetic SYN-MM-C05`.

Preconditions: All eight Demo Business Rules disabled; strategic baseline-15 restored. O03 belongs to A06; C04 is the primary Decision Maker and C05 is the non-primary Technical Buyer.

| Step | Action | Expected result |
|---:|---|---|
| 1 | Open O03 and find Related > Contact Roles. If the app's custom record page has no Related tab, open the same Opportunity in the standard Sales app. | There are two roles: C04 Decision Maker (primary), C05 Technical Buyer. |
| 2 | Use Edit Contact Roles or the related-list edit action. Make C05 the primary contact and change its role to Economic Buyer. | After saving, C05 is primary and its role is Economic Buyer. |
| 3 | Reload the Contact Roles list and count the primary flags. | Exactly two contacts remain linked and exactly one is primary; C04 is no longer primary. |
| 4 | Open C04 and C05 and inspect their Account links. | Both still belong to A06. Changing deal roles does not reparent the Contacts. |
| 5 | Inspect O03's strategic policy/history. | No policy-input changes were made, so the policy result and evaluation count remain unchanged. |

Negative/safety check: Do not add a duplicate role for the same Opportunity/Contact pair. If the UI cannot edit roles, report access/layout behavior rather than change global security.

Cleanup and repeatability: Restore C04 as primary Decision Maker and C05 as non-primary Technical Buyer.

Capture: Both contact roles, the single primary flag and unchanged Contact accounts.

Manual result: NOT RUN. Copy this case ID into the run ledger; generated specifications never certify execution.

<a id="mm-06"></a>

## MM-06 — Customer support case triage and closure

Objective: Verify manual case triage, priority changes and resolution while preserving the customer/contact links.

Actor: Original demo administrator (service-operator stand-in)

Fixtures: `SYN-MM-S01 Support`, `SYN-MM-A05 Customer`, `Synthetic SYN-MM-C01`.

Preconditions: All eight Demo Business Rules disabled; strategic baseline-15 restored. S01: New, Medium, Origin Web, Account A05, Contact C01. No custom SLA or automatic escalation rule is part of this scenario.

| Step | Action | Expected result |
|---:|---|---|
| 1 | Open Cases; choose All Cases and find Subject SYN-MM-S01 Support. | Status New, Priority Medium, Account A05 and Contact C01 are visible. |
| 2 | Edit Status to Working and Priority to High, then save. | The case persists as Working/High; account and contact remain unchanged. |
| 3 | Open the Account and Contact from the Case and inspect their related cases. | The same S01 is associated with the intended synthetic customer/contact. |
| 4 | Return to the Case and set Status to Closed (or use Close Case). Leave any optional email-notification checkbox off. | Case is closed; the record is retained and relationships are unchanged. |
| 5 | Reload the record; compare open versus all-case lists. | It no longer qualifies for an open-case list but remains available in All Cases. |

Negative/safety check: Escalation here is manual status/priority management, not a timed SLA, email-to-case integration or real customer notification.

Cleanup and repeatability: Reopen by restoring Status New and Priority Medium. Keep the Description marker and links unchanged.

Capture: Working/High and Closed states, case number/subject, and linked Account/Contact.

Manual result: NOT RUN. Copy this case ID into the run ledger; generated specifications never certify execution.

<a id="mm-07"></a>

## MM-07 — Case follow-up task and activity completion

Objective: Verify an assigned follow-up task is linked to both the correct case and person and moves to completed activity.

Actor: Original demo administrator

Fixtures: `SYN-MM-T41 Follow Up`, `SYN-MM-S01 Support`, `Synthetic SYN-MM-C01`.

Preconditions: All eight Demo Business Rules disabled; strategic baseline-15 restored. T41: Not Started, Normal priority, due 2026-12-15, Assigned To current operator, Related To S01, Name C01.

| Step | Action | Expected result |
|---:|---|---|
| 1 | Open Tasks, use an all-tasks list, and search SYN-MM-T41 Follow Up. | Confirm the expected assignee, due date, Related To case and Name contact. |
| 2 | Open the Related To case and check its activity timeline; return to the Task. | The task is associated with S01, not another support case. |
| 3 | Change Task Status to In Progress and save. | Status persists; the due date and relationships do not change. |
| 4 | Mark the task Complete or edit Status to Completed. | Task is Completed and is shown among past/completed activities. |
| 5 | Open S01 and C01 and inspect available activity timelines, expanding completed activity if needed. | The completed follow-up still refers to the same case/contact; it was not deleted. |

Negative/safety check: Do not use Send Email or assign the task to a real unrelated user. Completing a task does not automatically close its Case.

Cleanup and repeatability: Restore Task Status Not Started. Retain the original due date, owner and relationships.

Capture: Task relationship fields plus the completed activity state.

Manual result: NOT RUN. Copy this case ID into the run ledger; generated specifications never certify execution.

<a id="mm-08"></a>

## MM-08 — Campaign member response tracking

Objective: Verify changing one member's response updates campaign response tracking without duplicating members or sending a message.

Actor: Original demo administrator with the user-approved Marketing User setting

Fixtures: `SYN-MM-M01 Outreach`, `Synthetic SYN-MM-C01`, `SYN-MM-M03 Outreach`.

Preconditions: All eight Demo Business Rules disabled; strategic baseline-15 restored. Four Campaigns exist, with ten members each. M01/M02 have Contacts; M03/M04 have Leads. All forty members initially have Status Sent.

| Step | Action | Expected result |
|---:|---|---|
| 1 | Open Campaigns, choose All Campaigns and open SYN-MM-M01 Outreach. | Campaign is active and Planned; its Campaign Members list contains ten Contacts. |
| 2 | Find Synthetic SYN-MM-C01 in Campaign Members and open Edit for that member. | The membership status is Sent. This is a status label, not evidence an email was sent. |
| 3 | Change only the member Status to Responded and save. | The same member is now Responded; member count remains ten. |
| 4 | Refresh campaign statistics and the Campaign Members list. | One member is responded (initially none); membership count did not increase. |
| 5 | Open C01 and check Campaign History, then inspect M03's member list. | C01 remains linked to M01. M03 contains ten Lead members, demonstrating both supported person types. |

Negative/safety check: Do not click Send List Email or any campaign-send action. This use case does not send marketing communications.

Cleanup and repeatability: Restore C01's M01 membership Status to Sent; response count returns to its prior value.

Capture: Member before/after status and unchanged total membership count.

Manual result: NOT RUN. Copy this case ID into the run ledger; generated specifications never certify execution.

<a id="mm-09"></a>

## MM-09 — User separation, assigned approval and rejection

Objective: Verify the owner submits a deal, the separate VP can decide it but cannot edit its inputs, and rejection preserves native history.

Actor: Two sessions: original demo owner/administrator and Synthetic Regional VP

Fixtures: `SYN-MM-O02 Deal`, `Synthetic Regional VP`.

Preconditions: All eight Demo Business Rules disabled; strategic baseline-15 restored. O02: USD 60m, 20% discount, strategic=true, VP lookup set, Pending Regional VP policy eligibility, no native request initially. The VP uses the activated account and separate browser session.

| Step | Action | Expected result |
|---:|---|---|
| 1 | As owner, open O02 and confirm the input values and VP lookup. Click Submit for Approval in Regional VP Approval. | Latest native request becomes Pending. The owner is offered Recall, not the app's Approve/Reject. |
| 2 | In the separate VP session, open the same Opportunity and refresh the approval panel. | The assigned VP sees Approve and Reject. |
| 3 | Inspect the deal-input area as VP. Do not attempt to work around its permissions. | The VP cannot save input changes. Save and Evaluate may still be visible because the current workbench does not hide it by role; failed saving must not change the record. |
| 4 | As VP, click Reject. | Native request and Opportunity approval status become Rejected. The final decision is recorded and the ordinary-user approval lock is released. |
| 5 | As owner, refresh the page and try to locate a valid Submit action without changing inputs. | The app does not offer an unchanged rejected record for resubmission. History shows rejection. |
| 6 | As owner, change Discount to 21 and Save and Evaluate; submit the newly eligible deal; then Recall Approval as the owner. | A fresh evaluation yields Pending Regional VP; a second native request can be submitted and recalled. Its native status becomes Removed and policy status returns to Pending Regional VP. |

Negative/safety check: Never demonstrate owner/approver separation using an administrator override. This demo owner is an admin, so ordinary-owner edit locking is established by the separate Apex security tests, not this owner's UI session.

Cleanup and repeatability: Ensure there is no pending request. Restore Discount to 20 via Save and Evaluate. Native history and extra evaluation rows remain intentionally; do not delete them.

Capture: Separate-session action availability, rejection, second-request recall and final eligibility. No passwords or activation links.

Manual result: NOT RUN. Copy this case ID into the run ledger; generated specifications never certify execution.

<a id="mm-10"></a>

## MM-10 — Customer 360 relationship reconciliation

Objective: Trace a customer's sales, service, buying-team and follow-up records and verify counts/relationships across modules.

Actor: Original demo administrator

Fixtures: `SYN-MM-A05 Customer`, `SYN-MM-A01 Group`, `Synthetic SYN-MM-C01 through C03`, `SYN-MM-O01 Deal`, `SYN-MM-O02 Deal`, `SYN-MM-S01 through S03`.

Preconditions: All eight Demo Business Rules disabled; strategic baseline-15 restored. Run before mutations or after their documented cleanup. These are native related lists, not a newly built Customer 360 dashboard.

| Step | Action | Expected result |
|---:|---|---|
| 1 | Open A05 and inspect Details and Related lists. | Parent is A01; there are three seeded Contacts (C01–C03), two Opportunities (O01–O02) and three Cases (S01–S03). |
| 2 | Open both Opportunities and verify Account, Amount, and Contact Roles. | Both belong to A05. Amounts are USD 101,000 and USD 60,000,000, totaling USD 60,101,000. Each has C01 and C02 as its two buying-team contacts. |
| 3 | Open each of the three Cases and follow its Contact link. | S01→C01, S02→C02, S03→C03; all three Cases also point to A05. |
| 4 | Find tasks T01–T03, T21–T22 and T41–T43 in Tasks and inspect their relationship fields. | Eight seeded tasks connect to this customer: three directly to A05, two to its deals and three to its cases. These may not all aggregate into one Account timeline. |
| 5 | Inspect C01–C03's Campaign History or M01's Campaign Members list. | All three are members of M01. No duplicate Contact record is required to link sales, service and marketing. |
| 6 | Record the counts and compare every link with the expected graph. | All joins resolve to the same synthetic customer. A similarly named unrelated record is a failure, even if aggregate counts happen to match. |

Negative/safety check: Do not treat record-name resemblance as proof of a relationship. Open the lookup links; do not assume account-level automatic activity or campaign rollups.

Cleanup and repeatability: Read-only test; no cleanup needed. Restore earlier scenario mutations before claiming a pristine baseline.

Capture: Related-list counts, lookup links and the USD 60,101,000 two-deal total.

Manual result: NOT RUN. Copy this case ID into the run ledger; generated specifications never certify execution.

<a id="policy-01"></a>

## POLICY-01 — Non Strategic

Objective: Verify OBL-NON-STRATEGIC using an independent baseline expectation, without editing an existing approved fixture.

Actor: Original demo owner

Fixtures: `New Opportunity SYN-RULE-POLICY-01-{run}`, `SYN-MM-A05 Customer`, `Synthetic Regional VP`.

Preconditions: All eight new rules disabled. Strategic Default: Active=true, minimum=50000000 USD, discount=15, version=baseline-15. Unique run suffix; no pending approval on new record.

Exact data:

```json
{
  "Amount": 60000000,
  "Discount__c": 20,
  "Strategic_Deal__c": false,
  "approverPresent": true,
  "expectedStatus": "Not Required"
}
```

| Step | Action | Expected result |
|---:|---|---|
| 1 | Open App Launcher > Strategic Deal Assurance > Opportunities. Verify Default configuration in a separate admin Setup tab. | Exact baseline is present; do not derive expected answer from a changed live threshold. |
| 2 | Click New (or workbench New). Name=SYN-RULE-POLICY-01-{run}; Account=SYN-MM-A05 Customer; Stage=Prospecting; Close Date=2026-12-31. | New draft has exact synthetic identity; {run} uniquely identifies this execution. |
| 3 | Set Amount=60000000, Discount=20, Strategic Deal=false; Regional VP Approver=Synthetic Regional VP. Save. | Stored inputs match; Approval Status=Not Required; Rule Version=baseline-15. |
| 4 | Open Strategic Deal Policy, refresh, inspect latest Evaluation History. | One initial Flow evaluation matches inputs and Not Required. No native approval request created merely by evaluating. |
| 5 | Reload the same record; save again without changing inputs; refresh history. | Outcome persists; unchanged save adds no evaluation. No error is mistaken for a completed save. |
| 6 | Record actual vs expected values and evidence type in run result. | Result Passed only if every assertion observed; otherwise Failed or Blocked with reason. |

Negative/safety check: Never PATCH derived approval fields. Exact amount/discount boundaries use strict >, not >=. Blank differs from zero.

Cleanup and repeatability: Retain new synthetic run deal and evidence. Do not submit unless following a separate approval case. No original SYN-SDA record is overwritten.

Capture: Inputs, stored outcome/version, latest history, independent expectation, run suffix, actor role; no secrets.

Manual result: NOT RUN. Copy this case ID into the run ledger; generated specifications never certify execution.

<a id="policy-02"></a>

## POLICY-02 — Exact Amount

Objective: Verify OBL-AMOUNT-EXACT using an independent baseline expectation, without editing an existing approved fixture.

Actor: Original demo owner

Fixtures: `New Opportunity SYN-RULE-POLICY-02-{run}`, `SYN-MM-A05 Customer`, `Synthetic Regional VP`.

Preconditions: All eight new rules disabled. Strategic Default: Active=true, minimum=50000000 USD, discount=15, version=baseline-15. Unique run suffix; no pending approval on new record.

Exact data:

```json
{
  "Amount": 50000000,
  "Discount__c": 20,
  "Strategic_Deal__c": true,
  "approverPresent": true,
  "expectedStatus": "Not Required"
}
```

| Step | Action | Expected result |
|---:|---|---|
| 1 | Open App Launcher > Strategic Deal Assurance > Opportunities. Verify Default configuration in a separate admin Setup tab. | Exact baseline is present; do not derive expected answer from a changed live threshold. |
| 2 | Click New (or workbench New). Name=SYN-RULE-POLICY-02-{run}; Account=SYN-MM-A05 Customer; Stage=Prospecting; Close Date=2026-12-31. | New draft has exact synthetic identity; {run} uniquely identifies this execution. |
| 3 | Set Amount=50000000, Discount=20, Strategic Deal=true; Regional VP Approver=Synthetic Regional VP. Save. | Stored inputs match; Approval Status=Not Required; Rule Version=baseline-15. |
| 4 | Open Strategic Deal Policy, refresh, inspect latest Evaluation History. | One initial Flow evaluation matches inputs and Not Required. No native approval request created merely by evaluating. |
| 5 | Reload the same record; save again without changing inputs; refresh history. | Outcome persists; unchanged save adds no evaluation. No error is mistaken for a completed save. |
| 6 | Record actual vs expected values and evidence type in run result. | Result Passed only if every assertion observed; otherwise Failed or Blocked with reason. |

Negative/safety check: Never PATCH derived approval fields. Exact amount/discount boundaries use strict >, not >=. Blank differs from zero.

Cleanup and repeatability: Retain new synthetic run deal and evidence. Do not submit unless following a separate approval case. No original SYN-SDA record is overwritten.

Capture: Inputs, stored outcome/version, latest history, independent expectation, run suffix, actor role; no secrets.

Manual result: NOT RUN. Copy this case ID into the run ledger; generated specifications never certify execution.

<a id="policy-03"></a>

## POLICY-03 — Exact 15 Percent

Objective: Verify OBL-DISCOUNT-EXACT using an independent baseline expectation, without editing an existing approved fixture.

Actor: Original demo owner

Fixtures: `New Opportunity SYN-RULE-POLICY-03-{run}`, `SYN-MM-A05 Customer`, `Synthetic Regional VP`.

Preconditions: All eight new rules disabled. Strategic Default: Active=true, minimum=50000000 USD, discount=15, version=baseline-15. Unique run suffix; no pending approval on new record.

Exact data:

```json
{
  "Amount": 50000000.01,
  "Discount__c": 15,
  "Strategic_Deal__c": true,
  "approverPresent": true,
  "expectedStatus": "Not Required"
}
```

| Step | Action | Expected result |
|---:|---|---|
| 1 | Open App Launcher > Strategic Deal Assurance > Opportunities. Verify Default configuration in a separate admin Setup tab. | Exact baseline is present; do not derive expected answer from a changed live threshold. |
| 2 | Click New (or workbench New). Name=SYN-RULE-POLICY-03-{run}; Account=SYN-MM-A05 Customer; Stage=Prospecting; Close Date=2026-12-31. | New draft has exact synthetic identity; {run} uniquely identifies this execution. |
| 3 | Set Amount=50000000.01, Discount=15, Strategic Deal=true; Regional VP Approver=Synthetic Regional VP. Save. | Stored inputs match; Approval Status=Not Required; Rule Version=baseline-15. |
| 4 | Open Strategic Deal Policy, refresh, inspect latest Evaluation History. | One initial Flow evaluation matches inputs and Not Required. No native approval request created merely by evaluating. |
| 5 | Reload the same record; save again without changing inputs; refresh history. | Outcome persists; unchanged save adds no evaluation. No error is mistaken for a completed save. |
| 6 | Record actual vs expected values and evidence type in run result. | Result Passed only if every assertion observed; otherwise Failed or Blocked with reason. |

Negative/safety check: Never PATCH derived approval fields. Exact amount/discount boundaries use strict >, not >=. Blank differs from zero.

Cleanup and repeatability: Retain new synthetic run deal and evidence. Do not submit unless following a separate approval case. No original SYN-SDA record is overwritten.

Capture: Inputs, stored outcome/version, latest history, independent expectation, run suffix, actor role; no secrets.

Manual result: NOT RUN. Copy this case ID into the run ledger; generated specifications never certify execution.

<a id="policy-04"></a>

## POLICY-04 — Above 15 Percent

Objective: Verify OBL-DISCOUNT-ABOVE using an independent baseline expectation, without editing an existing approved fixture.

Actor: Original demo owner

Fixtures: `New Opportunity SYN-RULE-POLICY-04-{run}`, `SYN-MM-A05 Customer`, `Synthetic Regional VP`.

Preconditions: All eight new rules disabled. Strategic Default: Active=true, minimum=50000000 USD, discount=15, version=baseline-15. Unique run suffix; no pending approval on new record.

Exact data:

```json
{
  "Amount": 50000000.01,
  "Discount__c": 15.01,
  "Strategic_Deal__c": true,
  "approverPresent": true,
  "expectedStatus": "Pending Regional VP"
}
```

| Step | Action | Expected result |
|---:|---|---|
| 1 | Open App Launcher > Strategic Deal Assurance > Opportunities. Verify Default configuration in a separate admin Setup tab. | Exact baseline is present; do not derive expected answer from a changed live threshold. |
| 2 | Click New (or workbench New). Name=SYN-RULE-POLICY-04-{run}; Account=SYN-MM-A05 Customer; Stage=Prospecting; Close Date=2026-12-31. | New draft has exact synthetic identity; {run} uniquely identifies this execution. |
| 3 | Set Amount=50000000.01, Discount=15.01, Strategic Deal=true; Regional VP Approver=Synthetic Regional VP. Save. | Stored inputs match; Approval Status=Pending Regional VP; Rule Version=baseline-15. |
| 4 | Open Strategic Deal Policy, refresh, inspect latest Evaluation History. | One initial Flow evaluation matches inputs and Pending Regional VP. No native approval request created merely by evaluating. |
| 5 | Reload the same record; save again without changing inputs; refresh history. | Outcome persists; unchanged save adds no evaluation. No error is mistaken for a completed save. |
| 6 | Record actual vs expected values and evidence type in run result. | Result Passed only if every assertion observed; otherwise Failed or Blocked with reason. |

Negative/safety check: Never PATCH derived approval fields. Exact amount/discount boundaries use strict >, not >=. Blank differs from zero.

Cleanup and repeatability: Retain new synthetic run deal and evidence. Do not submit unless following a separate approval case. No original SYN-SDA record is overwritten.

Capture: Inputs, stored outcome/version, latest history, independent expectation, run suffix, actor role; no secrets.

Manual result: NOT RUN. Copy this case ID into the run ledger; generated specifications never certify execution.

<a id="policy-05"></a>

## POLICY-05 — Missing Approver

Objective: Verify OBL-MISSING-APPROVER using an independent baseline expectation, without editing an existing approved fixture.

Actor: Original demo owner

Fixtures: `New Opportunity SYN-RULE-POLICY-05-{run}`, `SYN-MM-A05 Customer`, `Synthetic Regional VP`.

Preconditions: All eight new rules disabled. Strategic Default: Active=true, minimum=50000000 USD, discount=15, version=baseline-15. Unique run suffix; no pending approval on new record.

Exact data:

```json
{
  "Amount": 50000000.01,
  "Discount__c": 15.01,
  "Strategic_Deal__c": true,
  "approverPresent": false,
  "expectedStatus": "Configuration Error"
}
```

| Step | Action | Expected result |
|---:|---|---|
| 1 | Open App Launcher > Strategic Deal Assurance > Opportunities. Verify Default configuration in a separate admin Setup tab. | Exact baseline is present; do not derive expected answer from a changed live threshold. |
| 2 | Click New (or workbench New). Name=SYN-RULE-POLICY-05-{run}; Account=SYN-MM-A05 Customer; Stage=Prospecting; Close Date=2026-12-31. | New draft has exact synthetic identity; {run} uniquely identifies this execution. |
| 3 | Set Amount=50000000.01, Discount=15.01, Strategic Deal=true; Regional VP Approver=blank (clear). Save. | Stored inputs match; Approval Status=Configuration Error; Rule Version=baseline-15. |
| 4 | Open Strategic Deal Policy, refresh, inspect latest Evaluation History. | One initial Flow evaluation matches inputs and Configuration Error. No native approval request created merely by evaluating. |
| 5 | Reload the same record; save again without changing inputs; refresh history. | Outcome persists; unchanged save adds no evaluation. No error is mistaken for a completed save. |
| 6 | Record actual vs expected values and evidence type in run result. | Result Passed only if every assertion observed; otherwise Failed or Blocked with reason. |

Negative/safety check: Never PATCH derived approval fields. Exact amount/discount boundaries use strict >, not >=. Blank differs from zero.

Cleanup and repeatability: Retain new synthetic run deal and evidence. Do not submit unless following a separate approval case. No original SYN-SDA record is overwritten.

Capture: Inputs, stored outcome/version, latest history, independent expectation, run suffix, actor role; no secrets.

Manual result: NOT RUN. Copy this case ID into the run ledger; generated specifications never certify execution.

<a id="policy-06"></a>

## POLICY-06 — Null Discount

Objective: Verify OBL-NULL-INPUT using an independent baseline expectation, without editing an existing approved fixture.

Actor: Original demo owner

Fixtures: `New Opportunity SYN-RULE-POLICY-06-{run}`, `SYN-MM-A05 Customer`, `Synthetic Regional VP`.

Preconditions: All eight new rules disabled. Strategic Default: Active=true, minimum=50000000 USD, discount=15, version=baseline-15. Unique run suffix; no pending approval on new record.

Exact data:

```json
{
  "Amount": 60000000,
  "Discount__c": null,
  "Strategic_Deal__c": true,
  "approverPresent": true,
  "expectedStatus": "Not Required"
}
```

| Step | Action | Expected result |
|---:|---|---|
| 1 | Open App Launcher > Strategic Deal Assurance > Opportunities. Verify Default configuration in a separate admin Setup tab. | Exact baseline is present; do not derive expected answer from a changed live threshold. |
| 2 | Click New (or workbench New). Name=SYN-RULE-POLICY-06-{run}; Account=SYN-MM-A05 Customer; Stage=Prospecting; Close Date=2026-12-31. | New draft has exact synthetic identity; {run} uniquely identifies this execution. |
| 3 | Set Amount=60000000, Discount=blank (clear), Strategic Deal=true; Regional VP Approver=Synthetic Regional VP. Save. | Stored inputs match; Approval Status=Not Required; Rule Version=baseline-15. |
| 4 | Open Strategic Deal Policy, refresh, inspect latest Evaluation History. | One initial Flow evaluation matches inputs and Not Required. No native approval request created merely by evaluating. |
| 5 | Reload the same record; save again without changing inputs; refresh history. | Outcome persists; unchanged save adds no evaluation. No error is mistaken for a completed save. |
| 6 | Record actual vs expected values and evidence type in run result. | Result Passed only if every assertion observed; otherwise Failed or Blocked with reason. |

Negative/safety check: Never PATCH derived approval fields. Exact amount/discount boundaries use strict >, not >=. Blank differs from zero.

Cleanup and repeatability: Retain new synthetic run deal and evidence. Do not submit unless following a separate approval case. No original SYN-SDA record is overwritten.

Capture: Inputs, stored outcome/version, latest history, independent expectation, run suffix, actor role; no secrets.

Manual result: NOT RUN. Copy this case ID into the run ledger; generated specifications never certify execution.

<a id="policy-07"></a>

## POLICY-07 — Null Amount

Objective: Verify OBL-NULL-INPUT using an independent baseline expectation, without editing an existing approved fixture.

Actor: Original demo owner

Fixtures: `New Opportunity SYN-RULE-POLICY-07-{run}`, `SYN-MM-A05 Customer`, `Synthetic Regional VP`.

Preconditions: All eight new rules disabled. Strategic Default: Active=true, minimum=50000000 USD, discount=15, version=baseline-15. Unique run suffix; no pending approval on new record.

Exact data:

```json
{
  "Amount": null,
  "Discount__c": 20,
  "Strategic_Deal__c": true,
  "approverPresent": true,
  "expectedStatus": "Not Required"
}
```

| Step | Action | Expected result |
|---:|---|---|
| 1 | Open App Launcher > Strategic Deal Assurance > Opportunities. Verify Default configuration in a separate admin Setup tab. | Exact baseline is present; do not derive expected answer from a changed live threshold. |
| 2 | Click New (or workbench New). Name=SYN-RULE-POLICY-07-{run}; Account=SYN-MM-A05 Customer; Stage=Prospecting; Close Date=2026-12-31. | New draft has exact synthetic identity; {run} uniquely identifies this execution. |
| 3 | Set Amount=blank (clear), Discount=20, Strategic Deal=true; Regional VP Approver=Synthetic Regional VP. Save. | Stored inputs match; Approval Status=Not Required; Rule Version=baseline-15. |
| 4 | Open Strategic Deal Policy, refresh, inspect latest Evaluation History. | One initial Flow evaluation matches inputs and Not Required. No native approval request created merely by evaluating. |
| 5 | Reload the same record; save again without changing inputs; refresh history. | Outcome persists; unchanged save adds no evaluation. No error is mistaken for a completed save. |
| 6 | Record actual vs expected values and evidence type in run result. | Result Passed only if every assertion observed; otherwise Failed or Blocked with reason. |

Negative/safety check: Never PATCH derived approval fields. Exact amount/discount boundaries use strict >, not >=. Blank differs from zero.

Cleanup and repeatability: Retain new synthetic run deal and evidence. Do not submit unless following a separate approval case. No original SYN-SDA record is overwritten.

Capture: Inputs, stored outcome/version, latest history, independent expectation, run suffix, actor role; no secrets.

Manual result: NOT RUN. Copy this case ID into the run ledger; generated specifications never certify execution.

<a id="rule-01"></a>

## RULE-01 — Account reparent blocked, then allowed by switch

Objective: Account reparent blocked, then allowed by switch

Actor: Original demo administrator; separate VP only where explicitly stated

Fixtures: `SYN-MM-A06 Customer`, `SYN-MM-A02 Group`, `SYN-MM-A01 Group`.

Preconditions: Run common preflight; restore baseline first. Keep other demo rules disabled. Use only the named synthetic records.

| Step | Action | Expected result |
|---:|---|---|
| 1 | Open A06 and record Parent=A02, contacts C04-C06 and deals O03-O04. | Baseline relationships match. |
| 2 | Setup > Custom Metadata Types > Demo Business Rule > Manage Records > Edit Account_Reparent. Set Enabled=true; Rule Version=demo-on-v1.  Save and reopen. | Saved configuration matches the requested values. Do not edit record API name or Protected. |
| 3 | Edit A06 Parent Account to A01 and Save. | Save is rejected with DEMO_RULE Account_Reparent. |
| 4 | Cancel unsaved changes and refresh A06; open hierarchy. | Parent remains A02; no contact/deal moved. |
| 5 | Edit only Description by appending a synthetic test note; Save. | Unrelated edit succeeds while rule is enabled; preserve original Description marker. |
| 6 | Setup > Custom Metadata Types > Demo Business Rule > Manage Records > Edit Account_Reparent. Set Enabled=false; Rule Version=baseline-off-v1.  Save and reopen. | Saved configuration matches the requested values. Do not edit record API name or Protected. |
| 7 | Change Parent to A01 and save again. | Save succeeds; A01 has six seeded children and A02 four; children of A06 unchanged. |

Negative/safety check: On rejection, refresh and verify stored values, not just the error toast. Do not bypass permissions or overwrite derived approval fields.

Cleanup and repeatability: Set Account_Reparent Enabled=false and Rule Version=baseline-off-v1; restore Number Value=100000 for Opportunity_Minimum or Required Value=Working - Contacted for Lead_Qualification. Restore A06 Parent=A02 and original Description; confirm original counts.

Capture: Record approved expected behavior, configuration before/after, exact synthetic fixture, save outcome, stored before/after values, error code and cleanup confirmation. Never capture secrets.

Manual result: NOT RUN. Copy this case ID into the run ledger; generated specifications never certify execution.

<a id="rule-02"></a>

## RULE-02 — Contact account change blocked and case links preserved

Objective: Contact account change blocked and case links preserved

Actor: Original demo administrator; separate VP only where explicitly stated

Fixtures: `Synthetic SYN-MM-C04`, `SYN-MM-A06 Customer`, `SYN-MM-A07 Customer`, `SYN-MM-S04 Support`.

Preconditions: Run common preflight; restore baseline first. Keep other demo rules disabled. Use only the named synthetic records.

| Step | Action | Expected result |
|---:|---|---|
| 1 | Open C04 and S04; record both Account=A06. | Starting customer links match. |
| 2 | Setup > Custom Metadata Types > Demo Business Rule > Manage Records > Edit Contact_Reparent. Set Enabled=true; Rule Version=demo-on-v1.  Save and reopen. | Saved configuration matches the requested values. Do not edit record API name or Protected. |
| 3 | Edit C04 Account to A07 and Save. | DEMO_RULE Contact_Reparent; reload shows A06. |
| 4 | Setup > Custom Metadata Types > Demo Business Rule > Manage Records > Edit Contact_Reparent. Set Enabled=false; Rule Version=baseline-off-v1.  Save and reopen. | Saved configuration matches the requested values. Do not edit record API name or Protected. |
| 5 | Repeat Contact Account=A07; save and inspect S04. | Contact moves to A07; existing Case Account remains A06, Contact remains C04. |
| 6 | Inspect A06/A07 Contact lists. | Seeded counts are two/four, with the same C04 ID. |

Negative/safety check: On rejection, refresh and verify stored values, not just the error toast. Do not bypass permissions or overwrite derived approval fields.

Cleanup and repeatability: Set Contact_Reparent Enabled=false and Rule Version=baseline-off-v1; restore Number Value=100000 for Opportunity_Minimum or Required Value=Working - Contacted for Lead_Qualification. Restore C04 Account=A06; counts three/three; do not change S04.

Capture: Record approved expected behavior, configuration before/after, exact synthetic fixture, save outcome, stored before/after values, error code and cleanup confirmation. Never capture secrets.

Manual result: NOT RUN. Copy this case ID into the run ledger; generated specifications never certify execution.

<a id="rule-03"></a>

## RULE-03 — Native lead conversion qualification gate and rollback

Objective: Native lead conversion qualification gate and rollback

Actor: Original demo administrator; separate VP only where explicitly stated

Fixtures: `New Lead SYN-RULE-LEAD-{run}`, `SYN-MM-A24 Customer`.

Preconditions: Run common preflight; restore baseline first. Keep other demo rules disabled. Use only the named synthetic records.

| Step | Action | Expected result |
|---:|---|---|
| 1 | In Leads click New: First Name=Synthetic, Last Name=SYN-RULE-LEAD-{run}, Company=Synthetic Rule Prospect, Status=Open - Not Contacted; save. | One new unconverted synthetic Lead. {run} is a unique locally recorded run suffix. |
| 2 | Setup > Custom Metadata Types > Demo Business Rule > Manage Records > Edit Lead_Qualification. Set Enabled=true; Rule Version=demo-on-v1. Required Value=Working - Contacted. Save and reopen. | Saved configuration matches the requested values. Do not edit record API name or Protected. |
| 3 | Record A24 Contact/Opportunity counts. Convert lead into existing A24, new Contact, new Opportunity SYN-RULE-LEAD-{run} Deal; leave notifications off. | Conversion is rejected with Lead_Qualification in the error. |
| 4 | Reload Lead and A24; search the requested Opportunity name. | Lead remains unconverted; no conversion Contact/Opportunity persisted and counts unchanged. |
| 5 | Edit Lead Status=Working - Contacted and Save before pressing Convert. | Qualification is persisted as a separate save. |
| 6 | Repeat the same conversion choices. | Conversion succeeds exactly once; new Contact/Opportunity link to A24; ordinary open-lead list excludes the converted Lead. |

Negative/safety check: On rejection, refresh and verify stored values, not just the error toast. Do not bypass permissions or overwrite derived approval fields.

Cleanup and repeatability: Set Lead_Qualification Enabled=false and Rule Version=baseline-off-v1; restore Number Value=100000 for Opportunity_Minimum or Required Value=Working - Contacted for Lead_Qualification. Conversion is consumed, not reversible by reset. Retain run records and ledger; use a fresh unique Lead next run. Customer counts increase intentionally.

Capture: Record approved expected behavior, configuration before/after, exact synthetic fixture, save outcome, stored before/after values, error code and cleanup confirmation. Never capture secrets.

Manual result: NOT RUN. Copy this case ID into the run ledger; generated specifications never certify execution.

<a id="rule-04"></a>

## RULE-04 — Closed Won minimum: 99999.99 USD is blocked

Objective: Closed Won minimum: 99999.99 USD is blocked

Actor: Original demo administrator; separate VP only where explicitly stated

Fixtures: `SYN-MM-O01 Deal`.

Preconditions: Run common preflight; restore baseline first. Keep other demo rules disabled. Use only the named synthetic records.

| Step | Action | Expected result |
|---:|---|---|
| 1 | Open O01; record Stage=Prospecting, Amount=101000, Strategic=false, Discount=5 and latest policy evaluation. | Baseline matches; no pending native approval. |
| 2 | Setup > Custom Metadata Types > Demo Business Rule > Manage Records > Edit Opportunity_Minimum. Set Enabled=true; Rule Version=demo-on-v1. Numeric Value=100000. Save and reopen. | Saved configuration matches the requested values. Do not edit record API name or Protected. |
| 3 | Set Amount=99999.99; keep Stage=Prospecting and save. | Open-stage save succeeds; Amount change appends policy evidence, still Not Required. |
| 4 | Record newest evaluation; change only Stage to Closed Won and Save. | DEMO_RULE Opportunity_Minimum; reload remains Prospecting. |
| 5 | Inspect Amount, policy and latest evaluation after the stage-only attempt. | Amount remains the value saved above; no additional strategic evaluation from a stage-only change/rejection. |

Negative/safety check: On rejection, refresh and verify stored values, not just the error toast. Do not bypass permissions or overwrite derived approval fields.

Cleanup and repeatability: Set Opportunity_Minimum Enabled=false and Rule Version=baseline-off-v1; restore Number Value=100000 for Opportunity_Minimum or Required Value=Working - Contacted for Lead_Qualification. Restore Stage=Prospecting then Amount=101000. Genuine Amount changes add history; do not delete evidence.

Capture: Record approved expected behavior, configuration before/after, exact synthetic fixture, save outcome, stored before/after values, error code and cleanup confirmation. Never capture secrets.

Manual result: NOT RUN. Copy this case ID into the run ledger; generated specifications never certify execution.

<a id="rule-05"></a>

## RULE-05 — Closed Won minimum: 100000 USD is allowed

Objective: Closed Won minimum: 100000 USD is allowed

Actor: Original demo administrator; separate VP only where explicitly stated

Fixtures: `SYN-MM-O01 Deal`.

Preconditions: Run common preflight; restore baseline first. Keep other demo rules disabled. Use only the named synthetic records.

| Step | Action | Expected result |
|---:|---|---|
| 1 | Open O01; record Stage=Prospecting, Amount=101000, Strategic=false, Discount=5 and latest policy evaluation. | Baseline matches; no pending native approval. |
| 2 | Setup > Custom Metadata Types > Demo Business Rule > Manage Records > Edit Opportunity_Minimum. Set Enabled=true; Rule Version=demo-on-v1. Numeric Value=100000. Save and reopen. | Saved configuration matches the requested values. Do not edit record API name or Protected. |
| 3 | Set Amount=100000; keep Stage=Prospecting and save. | Open-stage save succeeds; Amount change appends policy evidence, still Not Required. |
| 4 | Record newest evaluation; change only Stage to Closed Won and Save. | Save succeeds; reload shows Closed Won. |
| 5 | Inspect Amount, policy and latest evaluation after the stage-only attempt. | Amount remains the value saved above; no additional strategic evaluation from a stage-only change/rejection. |

Negative/safety check: On rejection, refresh and verify stored values, not just the error toast. Do not bypass permissions or overwrite derived approval fields.

Cleanup and repeatability: Set Opportunity_Minimum Enabled=false and Rule Version=baseline-off-v1; restore Number Value=100000 for Opportunity_Minimum or Required Value=Working - Contacted for Lead_Qualification. Restore Stage=Prospecting then Amount=101000. Genuine Amount changes add history; do not delete evidence.

Capture: Record approved expected behavior, configuration before/after, exact synthetic fixture, save outcome, stored before/after values, error code and cleanup confirmation. Never capture secrets.

Manual result: NOT RUN. Copy this case ID into the run ledger; generated specifications never certify execution.

<a id="rule-06"></a>

## RULE-06 — Closed Won minimum: 100000.01 USD is allowed

Objective: Closed Won minimum: 100000.01 USD is allowed

Actor: Original demo administrator; separate VP only where explicitly stated

Fixtures: `SYN-MM-O01 Deal`.

Preconditions: Run common preflight; restore baseline first. Keep other demo rules disabled. Use only the named synthetic records.

| Step | Action | Expected result |
|---:|---|---|
| 1 | Open O01; record Stage=Prospecting, Amount=101000, Strategic=false, Discount=5 and latest policy evaluation. | Baseline matches; no pending native approval. |
| 2 | Setup > Custom Metadata Types > Demo Business Rule > Manage Records > Edit Opportunity_Minimum. Set Enabled=true; Rule Version=demo-on-v1. Numeric Value=100000. Save and reopen. | Saved configuration matches the requested values. Do not edit record API name or Protected. |
| 3 | Set Amount=100000.01; keep Stage=Prospecting and save. | Open-stage save succeeds; Amount change appends policy evidence, still Not Required. |
| 4 | Record newest evaluation; change only Stage to Closed Won and Save. | Save succeeds; reload shows Closed Won. |
| 5 | Inspect Amount, policy and latest evaluation after the stage-only attempt. | Amount remains the value saved above; no additional strategic evaluation from a stage-only change/rejection. |

Negative/safety check: On rejection, refresh and verify stored values, not just the error toast. Do not bypass permissions or overwrite derived approval fields.

Cleanup and repeatability: Set Opportunity_Minimum Enabled=false and Rule Version=baseline-off-v1; restore Number Value=100000 for Opportunity_Minimum or Required Value=Working - Contacted for Lead_Qualification. Restore Stage=Prospecting then Amount=101000. Genuine Amount changes add history; do not delete evidence.

Capture: Record approved expected behavior, configuration before/after, exact synthetic fixture, save outcome, stored before/after values, error code and cleanup confirmation. Never capture secrets.

Manual result: NOT RUN. Copy this case ID into the run ledger; generated specifications never certify execution.

<a id="rule-07"></a>

## RULE-07 — Closed Won minimum: blank is blocked

Objective: Closed Won minimum: blank is blocked

Actor: Original demo administrator; separate VP only where explicitly stated

Fixtures: `SYN-MM-O01 Deal`.

Preconditions: Run common preflight; restore baseline first. Keep other demo rules disabled. Use only the named synthetic records.

| Step | Action | Expected result |
|---:|---|---|
| 1 | Open O01; record Stage=Prospecting, Amount=101000, Strategic=false, Discount=5 and latest policy evaluation. | Baseline matches; no pending native approval. |
| 2 | Setup > Custom Metadata Types > Demo Business Rule > Manage Records > Edit Opportunity_Minimum. Set Enabled=true; Rule Version=demo-on-v1. Numeric Value=100000. Save and reopen. | Saved configuration matches the requested values. Do not edit record API name or Protected. |
| 3 | Set Amount=blank (clear the field); keep Stage=Prospecting and save. | Open-stage save succeeds; Amount change appends policy evidence, still Not Required. |
| 4 | Record newest evaluation; change only Stage to Closed Won and Save. | DEMO_RULE Opportunity_Minimum; reload remains Prospecting. |
| 5 | Inspect Amount, policy and latest evaluation after the stage-only attempt. | Amount remains the value saved above; no additional strategic evaluation from a stage-only change/rejection. |

Negative/safety check: On rejection, refresh and verify stored values, not just the error toast. Do not bypass permissions or overwrite derived approval fields.

Cleanup and repeatability: Set Opportunity_Minimum Enabled=false and Rule Version=baseline-off-v1; restore Number Value=100000 for Opportunity_Minimum or Required Value=Working - Contacted for Lead_Qualification. Restore Stage=Prospecting then Amount=101000. Genuine Amount changes add history; do not delete evidence.

Capture: Record approved expected behavior, configuration before/after, exact synthetic fixture, save outcome, stored before/after values, error code and cleanup confirmation. Never capture secrets.

Manual result: NOT RUN. Copy this case ID into the run ledger; generated specifications never certify execution.

<a id="rule-08"></a>

## RULE-08 — Minimum changes immediately for the next save; switch-off restores permissive behavior

Objective: Minimum changes immediately for the next save; switch-off restores permissive behavior

Actor: Original demo administrator; separate VP only where explicitly stated

Fixtures: `SYN-MM-O01 Deal`.

Preconditions: Run common preflight; restore baseline first. Keep other demo rules disabled. Use only the named synthetic records.

| Step | Action | Expected result |
|---:|---|---|
| 1 | Confirm O01 Amount=101000 and Stage=Prospecting. | No approval request is pending. |
| 2 | Setup > Custom Metadata Types > Demo Business Rule > Manage Records > Edit Opportunity_Minimum. Set Enabled=true; Rule Version=demo-on-v1. Numeric Value=120000. Save and reopen. | Saved configuration matches the requested values. Do not edit record API name or Protected. |
| 3 | Set Stage=Closed Won and save. | Blocked by Opportunity_Minimum; remains Prospecting. |
| 4 | Setup > Custom Metadata Types > Demo Business Rule > Manage Records > Edit Opportunity_Minimum. Set Enabled=true; Rule Version=demo-on-v1. Numeric Value=100000; Rule Version=demo-min-100k. Save and reopen. | Saved configuration matches the requested values. Do not edit record API name or Protected. |
| 5 | Retry Stage=Closed Won. | Allowed with unchanged Amount=101000. |
| 6 | Reopen Stage=Prospecting; configure minimum=120000 but Enabled=false. | Saved amount remains 101000; rule is disabled. |
| 7 | Set Closed Won and save. | Allowed: disabled switch removes the extra restriction. |

Negative/safety check: On rejection, refresh and verify stored values, not just the error toast. Do not bypass permissions or overwrite derived approval fields.

Cleanup and repeatability: Set Opportunity_Minimum Enabled=false and Rule Version=baseline-off-v1; restore Number Value=100000 for Opportunity_Minimum or Required Value=Working - Contacted for Lead_Qualification. Restore O01 Stage=Prospecting, Amount=101000.

Capture: Record approved expected behavior, configuration before/after, exact synthetic fixture, save outcome, stored before/after values, error code and cleanup confirmation. Never capture secrets.

Manual result: NOT RUN. Copy this case ID into the run ledger; generated specifications never certify execution.

<a id="rule-09"></a>

## RULE-09 — Primary contact gate at Opportunity save

Objective: Primary contact gate at Opportunity save

Actor: Original demo administrator; separate VP only where explicitly stated

Fixtures: `New Opportunity SYN-RULE-PRIMARY-{run}`, `SYN-MM-A05 Customer`, `Synthetic SYN-MM-C01`.

Preconditions: Run common preflight; restore baseline first. Keep other demo rules disabled. Use only the named synthetic records.

| Step | Action | Expected result |
|---:|---|---|
| 1 | Create Opportunity SYN-RULE-PRIMARY-{run}: Account=A05, Stage=Prospecting, Close Date=2026-12-31, Amount=100000, Strategic=false, Discount=5; do not add contact roles. | One open deal exists with zero Contact Roles. |
| 2 | Setup > Custom Metadata Types > Demo Business Rule > Manage Records > Edit Opportunity_Primary_Contact. Set Enabled=true; Rule Version=demo-on-v1.  Save and reopen. | Saved configuration matches the requested values. Do not edit record API name or Protected. |
| 3 | Change Stage=Closed Won and save. | DEMO_RULE Opportunity_Primary_Contact; remains Prospecting. |
| 4 | Open the same record in standard Sales app > Related > Contact Roles. Add C01 as Decision Maker, Primary unchecked; save. | Exactly one non-primary role exists. |
| 5 | Retry Closed Won. | Still blocked: a contact role alone is insufficient. |
| 6 | Edit Contact Roles; mark C01 Primary; save. Retry Closed Won. | Save succeeds; exactly one primary role is present. |
| 7 | Refresh policy history; compare with before stage edits. | Stage/role-only edits add no strategic policy evaluation. |

Negative/safety check: On rejection, refresh and verify stored values, not just the error toast. Do not bypass permissions or overwrite derived approval fields.

Cleanup and repeatability: Set Opportunity_Primary_Contact Enabled=false and Rule Version=baseline-off-v1; restore Number Value=100000 for Opportunity_Minimum or Required Value=Working - Contacted for Lead_Qualification. Return new deal to Prospecting and retain it as a recorded run fixture. No deletion of roles or evidence required.

Capture: Record approved expected behavior, configuration before/after, exact synthetic fixture, save outcome, stored before/after values, error code and cleanup confirmation. Never capture secrets.

Manual result: NOT RUN. Copy this case ID into the run ledger; generated specifications never certify execution.

<a id="rule-10"></a>

## RULE-10 — Case resolution: blank and whitespace fail; text passes

Objective: Case resolution: blank and whitespace fail; text passes

Actor: Original demo administrator; separate VP only where explicitly stated

Fixtures: `SYN-MM-S01 Support`.

Preconditions: Run common preflight; restore baseline first. Keep other demo rules disabled. Use only the named synthetic records.

| Step | Action | Expected result |
|---:|---|---|
| 1 | Open S01 Details > Edit. Record New/Medium, Account=A05, Contact=C01; Demo Resolution blank. | Demo Resolution is visible to demo operator. If absent, report layout/FLS blocker. |
| 2 | Setup > Custom Metadata Types > Demo Business Rule > Manage Records > Edit Case_Resolution. Set Enabled=true; Rule Version=demo-on-v1.  Save and reopen. | Saved configuration matches the requested values. Do not edit record API name or Protected. |
| 3 | Set Status=Closed with Demo Resolution blank. Leave notification and assignment-rule options off; Save. | DEMO_RULE Case_Resolution; refresh shows original status. |
| 4 | Retry with Demo Resolution containing spaces only. | Still blocked; spaces are not a resolution. |
| 5 | Enter Demo Resolution=Synthetic connectivity issue resolved; verified with test customer. Set Closed and Save. | Closed persists with the exact resolution; Account/Contact unchanged; no custom notification is sent. |
| 6 | Setup > Custom Metadata Types > Demo Business Rule > Manage Records > Edit Case_Resolution. Set Enabled=false; Rule Version=baseline-off-v1.  Save and reopen. | Saved configuration matches the requested values. Do not edit record API name or Protected. |
| 7 | Clear Demo Resolution while leaving Closed and save. | Save succeeds when rule disabled. |

Negative/safety check: On rejection, refresh and verify stored values, not just the error toast. Do not bypass permissions or overwrite derived approval fields.

Cleanup and repeatability: Set Case_Resolution Enabled=false and Rule Version=baseline-off-v1; restore Number Value=100000 for Opportunity_Minimum or Required Value=Working - Contacted for Lead_Qualification. Restore Status=New, Priority=Medium, Demo Resolution blank. Do not clear seeded Description.

Capture: Record approved expected behavior, configuration before/after, exact synthetic fixture, save outcome, stored before/after values, error code and cleanup confirmation. Never capture secrets.

Manual result: NOT RUN. Copy this case ID into the run ledger; generated specifications never certify execution.

<a id="rule-11"></a>

## RULE-11 — Task completion requires a due date, not a future due date

Objective: Task completion requires a due date, not a future due date

Actor: Original demo administrator; separate VP only where explicitly stated

Fixtures: `SYN-MM-T41 Follow Up`, `SYN-MM-S01 Support`.

Preconditions: Run common preflight; restore baseline first. Keep other demo rules disabled. Use only the named synthetic records.

| Step | Action | Expected result |
|---:|---|---|
| 1 | Open T41; record Status=Not Started, Due Date=2026-12-15, Related To=S01, Name=C01. | Relationships match; Case status recorded. |
| 2 | Clear Due Date while task remains Not Started and save. | Allowed; task incomplete. |
| 3 | Setup > Custom Metadata Types > Demo Business Rule > Manage Records > Edit Task_Due_Date. Set Enabled=true; Rule Version=demo-on-v1.  Save and reopen. | Saved configuration matches the requested values. Do not edit record API name or Protected. |
| 4 | Mark Complete or set Status=Completed; save. | DEMO_RULE Task_Due_Date; reload remains incomplete. |
| 5 | Set Due Date=2026-01-01 and Status=Completed; save. | Allowed: presence is checked, not lateness/future date. |
| 6 | Inspect S01. | Task completes; Case is not automatically closed. |
| 7 | Setup > Custom Metadata Types > Demo Business Rule > Manage Records > Edit Task_Due_Date. Set Enabled=false; Rule Version=baseline-off-v1.  Save and reopen. | Saved configuration matches the requested values. Do not edit record API name or Protected. |
| 8 | Clear Due Date on the completed Task and save. | Allowed with rule disabled. |

Negative/safety check: On rejection, refresh and verify stored values, not just the error toast. Do not bypass permissions or overwrite derived approval fields.

Cleanup and repeatability: Set Task_Due_Date Enabled=false and Rule Version=baseline-off-v1; restore Number Value=100000 for Opportunity_Minimum or Required Value=Working - Contacted for Lead_Qualification. Restore Not Started and 2026-12-15; retain owner, WhoId, WhatId.

Capture: Record approved expected behavior, configuration before/after, exact synthetic fixture, save outcome, stored before/after values, error code and cleanup confirmation. Never capture secrets.

Manual result: NOT RUN. Copy this case ID into the run ledger; generated specifications never certify execution.

<a id="rule-12"></a>

## RULE-12 — Inactive campaign response blocked without sending messages

Objective: Inactive campaign response blocked without sending messages

Actor: Original demo administrator; separate VP only where explicitly stated

Fixtures: `SYN-MM-M01 Outreach`, `Synthetic SYN-MM-C01`.

Preconditions: Run common preflight; restore baseline first. Keep other demo rules disabled. Use only the named synthetic records.

| Step | Action | Expected result |
|---:|---|---|
| 1 | Open M01 and record Active=true, member count=10, C01 Status=Sent. | Baseline response count is zero if other tests were cleaned. |
| 2 | Edit Campaign Active=false and save. | Campaign becomes inactive; members unchanged. |
| 3 | Setup > Custom Metadata Types > Demo Business Rule > Manage Records > Edit Campaign_Active. Set Enabled=true; Rule Version=demo-on-v1.  Save and reopen. | Saved configuration matches the requested values. Do not edit record API name or Protected. |
| 4 | Edit C01's Campaign Member Status=Responded; save. | DEMO_RULE Campaign_Active; refresh shows Sent and unchanged response count. |
| 5 | Set Campaign Active=true; retry member Status=Responded. | Allowed; total members remains 10 and responses increase by one. |
| 6 | Set Campaign Active=false again. | No response changes occur just from deactivation. |
| 7 | Setup > Custom Metadata Types > Demo Business Rule > Manage Records > Edit Campaign_Active. Set Enabled=false; Rule Version=baseline-off-v1.  Save and reopen. | Saved configuration matches the requested values. Do not edit record API name or Protected. |
| 8 | Change C01 member Status=Sent; save. | Allowed while inactive because rule is disabled; no email-send action was used. |

Negative/safety check: On rejection, refresh and verify stored values, not just the error toast. Do not bypass permissions or overwrite derived approval fields.

Cleanup and repeatability: Set Campaign_Active Enabled=false and Rule Version=baseline-off-v1; restore Number Value=100000 for Opportunity_Minimum or Required Value=Working - Contacted for Lead_Qualification. Restore M01 Active=true and C01 member Status=Sent. Verify original counts.

Capture: Record approved expected behavior, configuration before/after, exact synthetic fixture, save outcome, stored before/after values, error code and cleanup confirmation. Never capture secrets.

Manual result: NOT RUN. Copy this case ID into the run ledger; generated specifications never certify execution.

<a id="rule-13"></a>

## RULE-13 — Two Opportunity guards compose without weakening either

Objective: Two Opportunity guards compose without weakening either

Actor: Original demo administrator; separate VP only where explicitly stated

Fixtures: `New Opportunity SYN-RULE-COMBINED-{run}`, `SYN-MM-A05 Customer`, `Synthetic SYN-MM-C01`.

Preconditions: Run common preflight; restore baseline first. Keep other demo rules disabled. Use only the named synthetic records.

| Step | Action | Expected result |
|---:|---|---|
| 1 | Create named open deal under A05: Prospecting, Amount=99999.99, Close Date=2026-12-31, Strategic=false, Discount=5, no roles. | Deal created once. |
| 2 | Setup > Custom Metadata Types > Demo Business Rule > Manage Records > Edit Opportunity_Minimum. Set Enabled=true; Rule Version=demo-on-v1. Numeric Value=100000. Save and reopen. | Saved configuration matches the requested values. Do not edit record API name or Protected. |
| 3 | Setup > Custom Metadata Types > Demo Business Rule > Manage Records > Edit Opportunity_Primary_Contact. Set Enabled=true; Rule Version=demo-on-v1.  Save and reopen. | Saved configuration matches the requested values. Do not edit record API name or Protected. |
| 4 | Attempt Stage=Closed Won. | Rejected; Salesforce may expose only the first rule error. Record remains Prospecting. Subsequent steps independently prove both guards. |
| 5 | Save Amount=100000 while open; retry Closed Won. | Still rejected by Opportunity_Primary_Contact. |
| 6 | Add primary C01 Decision Maker role using standard Sales app; retry Closed Won. | Allowed only after both conditions satisfied. |

Negative/safety check: On rejection, refresh and verify stored values, not just the error toast. Do not bypass permissions or overwrite derived approval fields.

Cleanup and repeatability: Set Opportunity_Minimum Enabled=false and Rule Version=baseline-off-v1; restore Number Value=100000 for Opportunity_Minimum or Required Value=Working - Contacted for Lead_Qualification. Set Opportunity_Primary_Contact Enabled=false and Rule Version=baseline-off-v1; restore Number Value=100000 for Opportunity_Minimum or Required Value=Working - Contacted for Lead_Qualification. Return created deal to Prospecting; keep synthetic run records.

Capture: Record approved expected behavior, configuration before/after, exact synthetic fixture, save outcome, stored before/after values, error code and cleanup confirmation. Never capture secrets.

Manual result: NOT RUN. Copy this case ID into the run ledger; generated specifications never certify execution.

<a id="rule-14"></a>

## RULE-14 — Invalid enabled configuration fails closed

Objective: Invalid enabled configuration fails closed

Actor: Original demo administrator; separate VP only where explicitly stated

Fixtures: `SYN-MM-O01 Deal`.

Preconditions: Run common preflight; restore baseline first. Keep other demo rules disabled. Use only the named synthetic records.

| Step | Action | Expected result |
|---:|---|---|
| 1 | Confirm O01 Stage=Prospecting and Amount=101000. | Baseline is ready. |
| 2 | Setup > Custom Metadata Types > Demo Business Rule > Manage Records > Edit Opportunity_Minimum. Set Enabled=true; Rule Version=demo-on-v1. Clear Numeric Value. Save and reopen. | Saved configuration matches the requested values. Do not edit record API name or Protected. |
| 3 | Attempt Stage=Closed Won. | DEMO_CONFIG; record unchanged. |
| 4 | Set Numeric Value=-1; retry Closed Won. | DEMO_CONFIG; negative minimum is invalid. |
| 5 | Set Numeric Value=100000 but clear Rule Version; retry Closed Won. | DEMO_CONFIG; enabled rule requires nonblank version. |
| 6 | Set Rule Version=demo-repaired-v1 and retry Closed Won. | Allowed. If testing a defect, agent must report mismatch against approved requirement before any repair. |

Negative/safety check: On rejection, refresh and verify stored values, not just the error toast. Do not bypass permissions or overwrite derived approval fields.

Cleanup and repeatability: Set Opportunity_Minimum Enabled=false and Rule Version=baseline-off-v1; restore Number Value=100000 for Opportunity_Minimum or Required Value=Working - Contacted for Lead_Qualification. Restore O01 Stage=Prospecting and Amount=101000. Never leave invalid configuration enabled.

Capture: Record approved expected behavior, configuration before/after, exact synthetic fixture, save outcome, stored before/after values, error code and cleanup confirmation. Never capture secrets.

Manual result: NOT RUN. Copy this case ID into the run ledger; generated specifications never certify execution.

<a id="rule-15"></a>

## RULE-15 — Lead required status is configurable

Objective: Lead required status is configurable

Actor: Original demo administrator; separate VP only where explicitly stated

Fixtures: `New Lead SYN-RULE-LEAD-CONFIG-{run}`, `SYN-MM-A24 Customer`.

Preconditions: Run common preflight; restore baseline first. Keep other demo rules disabled. Use only the named synthetic records.

| Step | Action | Expected result |
|---:|---|---|
| 1 | Create named Lead, Company=Synthetic Configuration Prospect, Status=Working - Contacted. | One unconverted Lead; use unique suffix. |
| 2 | Setup > Custom Metadata Types > Demo Business Rule > Manage Records > Edit Lead_Qualification. Set Enabled=true; Rule Version=demo-on-v1. Clear Required Value. Save and reopen. | Saved configuration matches the requested values. Do not edit record API name or Protected. |
| 3 | Attempt Convert to existing A24, new Contact, no Opportunity, notification off. | DEMO_CONFIG; no new Contact persists. |
| 4 | Set Required Value=Open - Not Contacted, version=demo-status-v2; retry. | Rejected with Lead_Qualification; saved prior status is Working - Contacted. |
| 5 | Save Lead Status=Open - Not Contacted, then retry conversion. | Allowed under the changed requirement; one new Contact linked to A24. |

Negative/safety check: On rejection, refresh and verify stored values, not just the error toast. Do not bypass permissions or overwrite derived approval fields.

Cleanup and repeatability: Set Lead_Qualification Enabled=false and Rule Version=baseline-off-v1; restore Number Value=100000 for Opportunity_Minimum or Required Value=Working - Contacted for Lead_Qualification. Consumed Lead stays converted; log new Contact and use another Lead next time.

Capture: Record approved expected behavior, configuration before/after, exact synthetic fixture, save outcome, stored before/after values, error code and cleanup confirmation. Never capture secrets.

Manual result: NOT RUN. Copy this case ID into the run ledger; generated specifications never certify execution.

<a id="rule-16"></a>

## RULE-16 — Renaming in the same save does not evade a reparent guard

Objective: Renaming in the same save does not evade a reparent guard

Actor: Original demo administrator; separate VP only where explicitly stated

Fixtures: `New Account SYN-RULE-SCOPE-{run}`, `SYN-MM-A01 Group`, `SYN-MM-A02 Group`.

Preconditions: Run common preflight; restore baseline first. Keep other demo rules disabled. Use only the named synthetic records.

| Step | Action | Expected result |
|---:|---|---|
| 1 | Create named synthetic Account with Parent=A01; record original Name. | Account is in synthetic scope. |
| 2 | Setup > Custom Metadata Types > Demo Business Rule > Manage Records > Edit Account_Reparent. Set Enabled=true; Rule Version=demo-on-v1.  Save and reopen. | Saved configuration matches the requested values. Do not edit record API name or Protected. |
| 3 | In one Edit form change Name=Synthetic scope probe {run} and Parent=A02; Save. | Blocked by Account_Reparent because the old name is in scope; both changes roll back. |
| 4 | Reload and verify original Name and Parent=A01. | No partial name change occurred. |

Negative/safety check: On rejection, refresh and verify stored values, not just the error toast. Do not bypass permissions or overwrite derived approval fields.

Cleanup and repeatability: Set Account_Reparent Enabled=false and Rule Version=baseline-off-v1; restore Number Value=100000 for Opportunity_Minimum or Required Value=Working - Contacted for Lead_Qualification. Retain named run Account; no record deletion. Prefix scoping is demo targeting, not a security boundary.

Capture: Record approved expected behavior, configuration before/after, exact synthetic fixture, save outcome, stored before/after values, error code and cleanup confirmation. Never capture secrets.

Manual result: NOT RUN. Copy this case ID into the run ledger; generated specifications never certify execution.

<a id="rule-17"></a>

## RULE-17 — Disabled rules preserve original workflows

Objective: Disabled rules preserve original workflows

Actor: Original demo administrator; separate VP only where explicitly stated

Fixtures: `Original SYN-MM fixtures`.

Preconditions: Run common preflight; restore baseline first. Keep other demo rules disabled. Use only the named synthetic records.

| Step | Action | Expected result |
|---:|---|---|
| 1 | Open all eight Demo Business Rule records; set Enabled=false and Rule Version=baseline-off-v1; restore default numeric/status values. | All eight records match baseline. |
| 2 | Execute MM-01 through MM-10 using their detailed steps and specified order; run conversions last. | Original native workflows remain valid; record results per case, not as one inferred pass. |
| 3 | Run read-only multi-module Verify after cleanup. | Restored seeded fields match; consumed lead conversions are recorded as intentional drift. New SYN-RULE run records are outside loader ownership. |

Negative/safety check: On rejection, refresh and verify stored values, not just the error toast. Do not bypass permissions or overwrite derived approval fields.

Cleanup and repeatability: Keep all eight switches off. Follow each MM cleanup; never automatically undo conversion or delete approval/evaluation history.

Capture: Record approved expected behavior, configuration before/after, exact synthetic fixture, save outcome, stored before/after values, error code and cleanup confirmation. Never capture secrets.

Manual result: NOT RUN. Copy this case ID into the run ledger; generated specifications never certify execution.

<a id="policy-08"></a>

## POLICY-08 — Discount configuration change from 15 to 20 with independent expected behavior

Objective: Discount configuration change from 15 to 20 with independent expected behavior

Actor: Original demo administrator; separate VP only where explicitly stated

Fixtures: `New Opportunities SYN-RULE-POLICY-BEFORE-{run} and SYN-RULE-POLICY-AFTER-{run}`, `SYN-MM-A05 Customer`, `Synthetic Regional VP`.

Preconditions: Run common preflight; restore baseline first. Keep other demo rules disabled. Use only the named synthetic records.

| Step | Action | Expected result |
|---:|---|---|
| 1 | Verify Strategic Discount Rule Default: Active=true, minimum=50000000, discount=15, version=baseline-15. | Baseline configuration is exact. |
| 2 | Create BEFORE deal: A05, Prospecting, Close Date=2026-12-31, Amount=60000000, Strategic=true, Discount=18, VP=Synthetic Regional VP. | Pending Regional VP eligibility with baseline-15; do not submit. |
| 3 | As admin edit Default: Discount Threshold Percent=20, Rule Version=demo-20. Leave other fields unchanged. | Configuration now 20; approved demo requirement determines whether this is intended change or injected defect. |
| 4 | Refresh BEFORE policy card; click unchanged Save and Evaluate. | Existing stored outcome/version does not automatically re-evaluate; stale baseline-15 remains detectable. |
| 5 | Create AFTER deal with the same input values and different unique name. | Not Required under demo-20; one initial evaluation. |
| 6 | Attempt app Submit for Approval on stale BEFORE deal if offered. | Current-policy recheck rejects stale eligibility; do not use standard approval API to bypass it. |

Negative/safety check: On rejection, refresh and verify stored values, not just the error toast. Do not bypass permissions or overwrite derived approval fields.

Cleanup and repeatability: Restore Default to Active=true, minimum=50000000, discount=15, version=baseline-15. Retain both run deals and snapshots; metadata restoration does not re-evaluate them.

Capture: Record approved expected behavior, configuration before/after, exact synthetic fixture, save outcome, stored before/after values, error code and cleanup confirmation. Never capture secrets.

Manual result: NOT RUN. Copy this case ID into the run ledger; generated specifications never certify execution.

<a id="policy-09"></a>

## POLICY-09 — Inactive strategic policy produces configuration error, not approval bypass

Objective: Inactive strategic policy produces configuration error, not approval bypass

Actor: Original demo administrator; separate VP only where explicitly stated

Fixtures: `New Opportunity SYN-RULE-INACTIVE-{run}`, `SYN-MM-A05 Customer`.

Preconditions: Run common preflight; restore baseline first. Keep other demo rules disabled. Use only the named synthetic records.

| Step | Action | Expected result |
|---:|---|---|
| 1 | Save Default Active=false while leaving baseline numbers/version unchanged. | Central policy explicitly inactive. |
| 2 | Create named deal: A05, Prospecting, Close Date=2026-12-31, Amount=60000000, Strategic=true, Discount=20, VP set. | Configuration Error; no native approval is created. Inactive does not mean Not Required. |
| 3 | Restore Default Active=true and genuinely change Discount=21 on the new deal; save. | Fresh evaluation is Pending Regional VP under baseline-15. Record both evaluations. |

Negative/safety check: On rejection, refresh and verify stored values, not just the error toast. Do not bypass permissions or overwrite derived approval fields.

Cleanup and repeatability: Restore strategic baseline; do not submit or delete the new deal.

Capture: Record approved expected behavior, configuration before/after, exact synthetic fixture, save outcome, stored before/after values, error code and cleanup confirmation. Never capture secrets.

Manual result: NOT RUN. Copy this case ID into the run ledger; generated specifications never certify execution.

<a id="approval-01"></a>

## APPROVAL-01 — Native approval approve with separate actors

Objective: Native approval approve with separate actors

Actor: Owner and separate Synthetic Regional VP; owner performs Recall only

Fixtures: `New Opportunity SYN-RULE-APPROVAL-01-{run}`, `SYN-MM-A05 Customer`, `Synthetic Regional VP`.

Preconditions: Run common preflight; restore baseline first. Keep other demo rules disabled. Use only the named synthetic records.

| Step | Action | Expected result |
|---:|---|---|
| 1 | As owner create named deal: A05, Prospecting, 2026-12-31, Amount=60000000, Strategic=true, Discount=20, VP=Synthetic Regional VP. | Eligibility Pending Regional VP; no request until explicit submit. |
| 2 | Record policy history; click Submit for Approval. | Latest native request Pending; owner sees Recall, not app Approve/Reject. |
| 3 | In separate Synthetic Regional VP session open same Opportunity, refresh approval and click Approve. | Native request and Opportunity status become Approved. |
| 4 | Reload as owner; compare policy history with pre-submit snapshot. | Native history records decision; no additional strategic evaluation from submit/decision/recall. |
| 5 | Confirm no pending native request remains. | Record unlocked for ordinary permitted edits; admin overrides are not used as security evidence. |

Negative/safety check: On rejection, refresh and verify stored values, not just the error toast. Do not bypass permissions or overwrite derived approval fields.

Cleanup and repeatability: Retain created deal and both history types. Restore baseline configuration; no pending requests left.

Capture: Record approved expected behavior, configuration before/after, exact synthetic fixture, save outcome, stored before/after values, error code and cleanup confirmation. Never capture secrets.

Manual result: NOT RUN. Copy this case ID into the run ledger; generated specifications never certify execution.

<a id="approval-02"></a>

## APPROVAL-02 — Native approval reject with separate actors

Objective: Native approval reject with separate actors

Actor: Owner and separate Synthetic Regional VP; owner performs Recall only

Fixtures: `New Opportunity SYN-RULE-APPROVAL-02-{run}`, `SYN-MM-A05 Customer`, `Synthetic Regional VP`.

Preconditions: Run common preflight; restore baseline first. Keep other demo rules disabled. Use only the named synthetic records.

| Step | Action | Expected result |
|---:|---|---|
| 1 | As owner create named deal: A05, Prospecting, 2026-12-31, Amount=60000000, Strategic=true, Discount=20, VP=Synthetic Regional VP. | Eligibility Pending Regional VP; no request until explicit submit. |
| 2 | Record policy history; click Submit for Approval. | Latest native request Pending; owner sees Recall, not app Approve/Reject. |
| 3 | In separate Synthetic Regional VP session open same Opportunity, refresh approval and click Reject. | Native request and Opportunity status become Rejected. |
| 4 | Reload as owner; compare policy history with pre-submit snapshot. | Native history records decision; no additional strategic evaluation from submit/decision/recall. |
| 5 | Confirm no pending native request remains. | Record unlocked for ordinary permitted edits; admin overrides are not used as security evidence. |

Negative/safety check: On rejection, refresh and verify stored values, not just the error toast. Do not bypass permissions or overwrite derived approval fields.

Cleanup and repeatability: Retain created deal and both history types. Restore baseline configuration; no pending requests left.

Capture: Record approved expected behavior, configuration before/after, exact synthetic fixture, save outcome, stored before/after values, error code and cleanup confirmation. Never capture secrets.

Manual result: NOT RUN. Copy this case ID into the run ledger; generated specifications never certify execution.

<a id="approval-03"></a>

## APPROVAL-03 — Native approval recall with separate actors

Objective: Native approval recall with separate actors

Actor: Owner and separate Synthetic Regional VP; owner performs Recall only

Fixtures: `New Opportunity SYN-RULE-APPROVAL-03-{run}`, `SYN-MM-A05 Customer`, `Synthetic Regional VP`.

Preconditions: Run common preflight; restore baseline first. Keep other demo rules disabled. Use only the named synthetic records.

| Step | Action | Expected result |
|---:|---|---|
| 1 | As owner create named deal: A05, Prospecting, 2026-12-31, Amount=60000000, Strategic=true, Discount=20, VP=Synthetic Regional VP. | Eligibility Pending Regional VP; no request until explicit submit. |
| 2 | Record policy history; click Submit for Approval. | Latest native request Pending; owner sees Recall, not app Approve/Reject. |
| 3 | As owner click Recall Approval. | Native status Removed; Opportunity returns Pending Regional VP. |
| 4 | Reload as owner; compare policy history with pre-submit snapshot. | Native history records decision; no additional strategic evaluation from submit/decision/recall. |
| 5 | Confirm no pending native request remains. | Record unlocked for ordinary permitted edits; admin overrides are not used as security evidence. |

Negative/safety check: On rejection, refresh and verify stored values, not just the error toast. Do not bypass permissions or overwrite derived approval fields.

Cleanup and repeatability: Retain created deal and both history types. Restore baseline configuration; no pending requests left.

Capture: Record approved expected behavior, configuration before/after, exact synthetic fixture, save outcome, stored before/after values, error code and cleanup confirmation. Never capture secrets.

Manual result: NOT RUN. Copy this case ID into the run ledger; generated specifications never certify execution.

<a id="security-01"></a>

## SECURITY-01 — VP read-only inputs and no metadata-admin access

Objective: VP read-only inputs and no metadata-admin access

Actor: Separate Synthetic Regional VP

Fixtures: `Any assigned SYN-RULE approval Opportunity`, `Synthetic Regional VP`.

Preconditions: Run common preflight; restore baseline first. Keep other demo rules disabled. Use only the named synthetic records.

| Step | Action | Expected result |
|---:|---|---|
| 1 | Sign in as separate VP, open an assigned synthetic Opportunity and record Amount/Discount. | VP can read assigned deal. |
| 2 | Inspect Deal Inputs and attempt a normal edit/save only if UI permits entering a changed Discount. | Save denied; refresh confirms original inputs. Visible Save and Evaluate does not grant edit rights. |
| 3 | Attempt to navigate to Setup Custom Metadata Types through normal UI navigation. | VP cannot manage demo rules; do not add permissions or use admin session as workaround. |
| 4 | Return to approval panel with a pending request assigned to this VP. | Approve/Reject remains available only for assigned request; separate permission boundary from editing inputs. |

Negative/safety check: On rejection, refresh and verify stored values, not just the error toast. Do not bypass permissions or overwrite derived approval fields.

Cleanup and repeatability: Discard unsaved edits; finish or recall the test request through its authorized actor.

Capture: Record approved expected behavior, configuration before/after, exact synthetic fixture, save outcome, stored before/after values, error code and cleanup confirmation. Never capture secrets.

Manual result: NOT RUN. Copy this case ID into the run ledger; generated specifications never certify execution.

<a id="security-02"></a>

## SECURITY-02 — Non-synthetic records remain outside demo guard scope

Objective: Non-synthetic records remain outside demo guard scope

Actor: Original demo administrator; separate VP only where explicitly stated

Fixtures: `New synthetic-content controls named Synthetic Control {run} (no SYN-MM-/SYN-RULE- prefix)`.

Preconditions: Run common preflight; restore baseline first. Keep other demo rules disabled. Use only the named synthetic records.

| Step | Action | Expected result |
|---:|---|---|
| 1 | Setup > Custom Metadata Types > Demo Business Rule > Manage Records > Edit Case_Resolution. Set Enabled=true; Rule Version=demo-on-v1.  Save and reopen. Setup > Custom Metadata Types > Demo Business Rule > Manage Records > Edit Task_Due_Date. Set Enabled=true; Rule Version=demo-on-v1.  Save and reopen. Setup > Custom Metadata Types > Demo Business Rule > Manage Records > Edit Opportunity_Minimum. Set Enabled=true; Rule Version=demo-on-v1. Numeric Value=100000. Save and reopen. | Three guards enabled. |
| 2 | Create a Case Subject=Synthetic Control Case {run}, Origin=Web, Status=Closed, Demo Resolution blank; notifications off. | New demo guard does not reject because prefix is outside scope; unrelated platform rules can still apply. |
| 3 | Create Task Subject=Synthetic Control Task {run}, Status=Completed, no Due Date, no external assignee. | New due-date guard does not reject. |
| 4 | Create Opportunity Name=Synthetic Control Deal {run}, Account=A05, Stage=Closed Won, Close Date=2026-12-31, Amount=1, Strategic=false. | New minimum guard does not reject; strategic policy Not Required. |

Negative/safety check: On rejection, refresh and verify stored values, not just the error toast. Do not bypass permissions or overwrite derived approval fields.

Cleanup and repeatability: Set Case_Resolution Enabled=false and Rule Version=baseline-off-v1; restore Number Value=100000 for Opportunity_Minimum or Required Value=Working - Contacted for Lead_Qualification. Set Task_Due_Date Enabled=false and Rule Version=baseline-off-v1; restore Number Value=100000 for Opportunity_Minimum or Required Value=Working - Contacted for Lead_Qualification. Set Opportunity_Minimum Enabled=false and Rule Version=baseline-off-v1; restore Number Value=100000 for Opportunity_Minimum or Required Value=Working - Contacted for Lead_Qualification. Retain these explicitly created synthetic-content controls in the run ledger. Never test against real records.

Capture: Record approved expected behavior, configuration before/after, exact synthetic fixture, save outcome, stored before/after values, error code and cleanup confirmation. Never capture secrets.

Manual result: NOT RUN. Copy this case ID into the run ledger; generated specifications never certify execution.

<a id="recovery-01"></a>

## RECOVERY-01 — Rule restoration and dataset reconciliation

Objective: Rule restoration and dataset reconciliation

Actor: Original demo administrator; separate VP only where explicitly stated

Fixtures: `All eight rule records`, `Strategic Discount Rule Default`, `Touched SYN-MM fixtures`.

Preconditions: Run common preflight; restore baseline first. Keep other demo rules disabled. Use only the named synthetic records.

| Step | Action | Expected result |
|---:|---|---|
| 1 | Finish/recall every pending test approval; record consumed conversions. | No pending request is silently canceled. |
| 2 | Disable all eight demo rules, restore baseline-off-v1 and default parameters; restore strategic 15% baseline. | Configuration is back to approved baseline. |
| 3 | Execute cleanup in reverse mutation order: campaigns/members, tasks, cases, Opportunity stages/amounts/roles, contact Accounts, account parents. | Touched reusable fixtures match their documented starting values; history is retained. |
| 4 | Run operator read-only Verify or inspect each named record and relationships in UI. | No unexplained drift; consumed Leads/new records documented. Create-only seeding is not a reset. |
| 5 | Capture before/after config and completion of every cleanup. | Automation may report clean only with explicit evidence, not because switches are disabled. |

Negative/safety check: On rejection, refresh and verify stored values, not just the error toast. Do not bypass permissions or overwrite derived approval fields.

Cleanup and repeatability: No deletion. Preserve run ledger, policy and native approval evidence.

Capture: Record approved expected behavior, configuration before/after, exact synthetic fixture, save outcome, stored before/after values, error code and cleanup confirmation. Never capture secrets.

Manual result: NOT RUN. Copy this case ID into the run ledger; generated specifications never certify execution.
